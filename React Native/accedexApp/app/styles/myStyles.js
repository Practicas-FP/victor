import { StyleSheet } from 'react-native'

export const myStyles = StyleSheet.create({
    container: {
        flex: 1,
        marginTop: 8,
        backgroundColor: "aliceblue",
    },
    row: {
        flexDirection: "row",
        flexWrap: "wrap",
    },
});